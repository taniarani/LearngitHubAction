name: Meta-Data Extraction
on: workflow_dispatch
jobs: 
  extract:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: '04 Practice Project (Start)'  # Folder expected at repo root; we will verify it exists
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Output GitHub Context
        run: |
          echo "GitHub Context JSON:" 
          echo '${{ toJSON(github) }}'
